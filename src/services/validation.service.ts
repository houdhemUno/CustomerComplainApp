export class ValidationService {

  // Méthode de validation des données des réclamations
  validateClaimData(claimData: any): boolean {
    return (
        // les conditions de validation 
    );
  }
}
